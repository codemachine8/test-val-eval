# Core testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# For randomizing test order (exposes order-dependent flakiness)
pytest-randomly>=3.12.0

# Optional: for retrying flaky tests during debugging
pytest-rerunfailures>=11.0

# Optional: for parallel test execution
pytest-xdist>=3.0.0
